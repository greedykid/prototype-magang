@extends('layouts.app') @section('title',$lpk->name.' | SIMASADI') @section('content')<div class="page-heading"><div><a class="back-link" href="{{ route('lpks.index') }}">Semua LPK</a><h1>{{ $lpk->name }}</h1><p class="lede">{{ $lpk->registration_number }} · data contoh lokal</p></div><a class="button secondary" href="{{ route('lpks.edit',$lpk) }}">Ubah data</a></div><div class="detail-grid"><section class="panel"><span class="eyebrow">INFORMASI DASAR</span><dl class="detail-list"><div><dt>Status</dt><dd><x-status :value="$lpk->status" /></dd></div><div><dt>Email</dt><dd>{{ $lpk->email ?: 'Belum diisi' }}</dd></div><div><dt>Telepon</dt><dd>{{ $lpk->phone ?: 'Belum diisi' }}</dd></div><div><dt>Alamat</dt><dd>{{ $lpk->address ?: 'Belum diisi' }}</dd></div><div><dt>Catatan</dt><dd>{{ $lpk->notes ?: 'Belum ada catatan.' }}</dd></div></dl></section><section class="panel"><div class="panel-head"><div><span class="eyebrow">AKREDITASI</span><h2>Proses terkait</h2></div><a href="{{ route('accreditations.index') }}">Semua proses</a></div>@forelse($lpk->accreditations as $item)<a class="list-row" href="{{ route('accreditations.show',$item) }}"><div><strong>Proses akreditasi #{{ $item->id }}</strong><span>Target {{ $item->target_date?->format('d M Y') ?: 'Belum ditentukan' }}</span></div><x-status :value="$item->status" /></a>@empty<div class="empty">Belum ada proses akreditasi.</div>@endforelse</section></div>@endsection
